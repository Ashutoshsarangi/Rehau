import { Component, h, Prop, Event, EventEmitter, Watch, State } from '@stencil/core';
import { OnBoardingModel } from '../../../interfaces/on-boarding-model';

@Component({
  tag: 'rh-home',
  styleUrl: 'rh-home.molecule.scss',
  shadow: true
})
export class RhHome {
  
}
