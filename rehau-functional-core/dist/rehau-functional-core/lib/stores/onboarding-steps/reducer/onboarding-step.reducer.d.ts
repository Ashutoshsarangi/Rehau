import { Action } from '@ngrx/store';
export declare const initialState: {
    configPosition: number;
};
export declare function onBoardingReducer(state: any | undefined, action: Action): {
    configPosition: any;
};
